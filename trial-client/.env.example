# FitBridge client env (optional).
#
# The app already ships with working PUBLIC Supabase credentials baked in as a
# fallback (see src/lib/supabase.js), so it runs with no config. Set these only
# if you want to point the build at a different Supabase project.
#
# These are the client-side "publishable"/anon keys — safe to expose. Row Level
# Security (see supabase/migrations) is what protects data.

VITE_SUPABASE_URL=https://wurkhgxhrqqhdzjzmawy.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_gKWhHrAd4JSMNfSYNEevkA_Nh292CYJ
